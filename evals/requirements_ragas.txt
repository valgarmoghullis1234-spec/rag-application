# RAGAS evaluation dependencies
# Install with: python -m pip install -r evals/requirements_ragas.txt
#
# NOTE: scikit-network (a ragas dependency) requires Microsoft C++ Build Tools on Windows.
# Install ragas without it — the LLM-based metrics we use don't need it:
#   python -m pip install ragas --no-deps
#   python -m pip install -r evals/requirements_ragas.txt
ragas==0.4.3
langchain-openai>=0.2.0
langchain>=1.0.0
langchain-community>=0.4.0
datasets>=2.14.0
appdirs
diskcache>=5.6.3
instructor
nest-asyncio
